[build]
builder = "dockerfile"

[deploy]
startCommand = "python -m app.training.seed_demo --if-empty && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
